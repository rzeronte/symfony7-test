# Symfony Docker

Instalación docker
```
mkdir symfony7-test
cd symfony7-test
git clone https://github.com/rzeronte/symfony7-test.git .
make build
make up
```

Reset de la aplicación
```
make reset-app
```
